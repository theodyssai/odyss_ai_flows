[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "odyss_ai_flows_cache"
version = "0.1.0"
authors = [
    { name = "Odyss AI" }
]
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["odyss_ai_flows_cache*"]
