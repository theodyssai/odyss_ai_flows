{
  "plain_value": "plain",
  "env_value": "{{ ENV('GLOBAL_TEST_ENV') }}",
  "config_value": "{{ CONFIG('abc') }}",
  "secret_value": "{{ VAULT('token') }}",
  "wrapped_env": "{{ SECRET(ENV('GLOBAL_TEST_ENV')) }}",
  "merged_dict": {
    "a": 1,
    "b": {
      "x": 2
    }
  }
}